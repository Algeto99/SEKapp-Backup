name: Mirror Source Repository

on:
  schedule:
    # Runs once every day at midnight (00:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows you to trigger it manually

# Grants the runner permission to push code into this backup repository
permissions:
  contents: write

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror Mirror on the Wall
        run: |
          # Configure Git
          git config --global user.name "Backup Bot"
          git config --global user.email "backup-bot@example.com"
          
          # 1. Clone the source repository as a bare mirror using your secret token
          git clone --mirror https://x-access-token:${{ secrets.SEKAPPSECRETO }}@github.com/rcanton/sekapp.git sekapp.git
          
          cd sekapp.git
          
          # 2. Push everything to your destination backup repository
          git push --mirror https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/Algeto99/SEKapp-Backup.git
